import java.util.*;;

public class Hashmap {
    // static void Display(Map<Integer,Integer> map){
    //     //Lambda Function
    //     map.forEach((key,value)->{
    //     });
    // }
    // static void countFrequency(int[] arr){
    //     Map<Integer, Integer> map =new HashMap<>();
    //     //populating our map
    //     for(int i=0; i<arr.length; i++){
    //       if(map.containsKey(arr[i]))
    //       map.put(arr[i],map.get(arr[i])+1);
    //       else
    //        map.put(arr[i], 1);
    //     }
    //     Display(map);
    // }
    // public static void main(String[] args) {
    //     //HW: How is sorting 
    //    int arr[]={4,2,3,4,5,6,7,8,9,89,66,55};
    //    countFrequency(arr);
       
       // String arr[] = { "Aditya", "Rahul", "Saurav", "Ajeet" };

        // creating the hashmap for the above array
        // Map<Integer, String> rollCall = new HashMap<>();
        // for (int i = 1; i <= arr.length; i++) {
        //     rollCall.put(i, arr[i - 1]);
        // }

        // // Display Our Hashmap
        // for (Map.Entry<Integer, String> entry : rollCall.entrySet()) {
        //     System.out.println(entry.getKey() + " " + entry.getValue());
        // }

    //   // question 2
    //     Map<Character, String> rollCall = new HashMap<>();
    //     for (char i = 0; i <arr.length; i++) {
    //         rollCall.put((char)(97+i),arr[i]);
    //     }

    //     // Display Our Hashmap
    //     for (Map.Entry<Character, String> entry : rollCall.entrySet()) {
    //         System.out.println(entry.getKey() + " " + entry.getValue());
         //}
           
        static void minAndMaxFrequency(int[] arr){
            
        }

     
        }
    
